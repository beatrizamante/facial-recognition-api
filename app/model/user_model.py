def data_processing() :
    return "This will handle all datastorage for user info and face encoding"

def __init__(self, image, userId): 
    self.

#This function will get the image of an user and compare to the database. If it exists, and its value is close to the label stored in the database, it will add it alongside the one already stored
def user_new_label_registration(image): 
    userId